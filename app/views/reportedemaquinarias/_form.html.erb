<%= form_for(@reportedemaquinaria) do |f| %>
  <% if @reportedemaquinaria.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reportedemaquinaria.errors.count, "error") %> prohibited this reportedemaquinaria from being saved:</h2>

      <ul>
      <% @reportedemaquinaria.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
