<%= form_for(@tareo) do |f| %>
  <% if @tareo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tareo.errors.count, "error") %> prohibited this tareo from being saved:</h2>

      <ul>
      <% @tareo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
