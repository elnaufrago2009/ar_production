<%= form_for(@tipodedia) do |f| %>
  <% if @tipodedia.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipodedia.errors.count, "error") %> prohibited this tipodedia from being saved:</h2>

      <ul>
      <% @tipodedia.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
