<%= form_for(@importalista) do |f| %>
  <% if @importalista.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@importalista.errors.count, "error") %> prohibited this importalista from being saved:</h2>

      <ul>
      <% @importalista.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
