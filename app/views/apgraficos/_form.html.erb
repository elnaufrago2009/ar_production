<%= form_for(@apgrafico) do |f| %>
  <% if @apgrafico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@apgrafico.errors.count, "error") %> prohibited this apgrafico from being saved:</h2>

      <ul>
      <% @apgrafico.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
